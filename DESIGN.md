# 课程资料库（course-library）设计方案 · 定稿

> 本地优先的网页资料库：把散落各处的课程文件，收进「**学期 → 课程 → 分类**」结构，可检索、可管版本、可一键打包。
> 数据全存浏览器本地（IndexedDB），**无账号、无服务器、不上传**。

- 定稿日期：2026-09-16
- 修订：2026-09-16 追加「本地文本提取 + 内容搜索」，并明确排除「接入 AI 生成复习笔记」（详见第七节）
- 定位：自己用 + 推荐给亲友，开源（MIT），GitHub Pages 免费部署
- 技术模板：记账本 ledger（<https://fcyzjbn.github.io/ledger>），技术栈与部署流程照抄它

---

## 一、一句话定位

把「散落的课程文件」变成「按 学期→课程→分类 组织、能检索、能管版本、能一键打包」的本地资料库。

**核心链路**：`导入 → 预览分类 → 确认入库 →（后台提取正文）→ 检索/管理 → 导出/备份`

---

## 二、已拍板决策（存档）

### 产品层面

| 项 | 决定 |
|---|---|
| 产品形态 | 网页 PWA（GitHub Pages 部署，手机可「添加到主屏幕」），非桌面应用 |
| 数据量 | ≤2GB，IndexedDB 本地存储 |
| 隐私 | 无账号、无服务器、零上传 |
| 名字 / 仓库 | 课程资料库 / `FCYZJBN/course-library` |
| 许可 | MIT 公开 |

### 结构层面

| 项 | 决定 |
|---|---|
| 层级 | 学期 → 课程 → 分类 → 文件（含学期层） |
| 课程字段 | 名称 + 所属学期（必填）；老师、颜色/图标（选填）；**不加**学分/教室/时间 |
| 分类 | 全局默认 5 类（课件/作业/笔记/真题/其他）+ 每门课可自定义 |
| 课程别名 | 建课自动带常用别名（高等数学→高数），可手补；分类器按「课程名+全部别名」匹配 |

### 交互层面

| 项 | 决定 |
|---|---|
| 导入 | 拖拽 / 选文件 / Chrome 选整个文件夹 → **预览分类 → 可手动改 → 确认才入库** |
| 未识别文件 | 归入「待整理」区，手动分配（**不硬猜**） |
| 版本 | 只打标（最新版高亮、旧版保留），**不做**删除/清理 |
| 手机端 | PC 主战场（导入/整理/导出）；手机主打「查 / 看 / 导出」 |
| 首次引导 | 空库 + 三步引导（建学期→建课→导入），**无示例数据按钮**；使用说明放 README/用户指南 |

### 备份与设备

| 项 | 决定 |
|---|---|
| 备份恢复 | 完整做：导出备份 zip（文件+元数据）+ 导入 zip 一键还原 |
| 多设备 | 各设备独立，靠「导出 zip → 手动搬 → 导入」；**不做**云同步 |

### 技术层面

| 项 | 决定 |
|---|---|
| 技术栈 | 原生 JS ES Module + IndexedDB + PWA + **JSZip** + **pdf.js**（仅提取 PDF 用，按需加载） |
| 不引入 | ECharts、任何前端框架、任何构建步骤 |
| 文本提取 | **本地提取** docx / pptx / xlsx / pdf / txt 正文，存库；**永不上传** |
| 内容搜索 | 支持按文件**正文关键词**搜索（中文用子串匹配，无需分词） |
| AI 总结 | **明确不做**（见第七节） |

---

## 三、数据模型（IndexedDB 七张表）

> **与定稿的一处实现修正**：方案里写的是四张表、`blob` 和 `textContent` 直接挂在 `files` 记录上。
> 实现时拆成了七张——`files` 只放元数据，文件本体进 `blobs`，提取出的正文进 `texts`。
> 原因是致命的：IndexedDB 的 `getAll('files')` 会把整条记录连同 `blob` 一起读进内存。
> 两三千份文件就是 2GB，启动时那一下直接把标签页卡死。
> 拆开之后，列目录只读几 KB 的元数据，正文扫描走 `openCursor` 流式读，内存占用与库大小无关。

| 表 | 关键字段 | 说明 |
|---|---|---|
| semesters 学期 | id, name, sortOrder, createdAt | 如「2025 秋」 |
| courses 课程 | id, name, aliases[], teacher?, color?, semesterId, sortOrder | aliases 是字符串数组 |
| categories 分类 | id, name, icon?, key?, sortOrder, courseId | 见下方说明 |
| files 文件 | id, courseId, categoryId, name, ext, size, mime, relPath, importedAt, versionLabel, extractStatus, textLength | 纯元数据，几百字节 |
| blobs 文件本体 | id, blob | id 与 files 一一对应 |
| texts 正文 | id, text | 提取出的纯文本，供内容搜索 |
| settings 设置 | key, value | 预留 |

- `versionLabel` ∈ `latest` / `old` / `none`；`extractStatus` ∈ `pending` / `done` / `empty` / `failed` / `unsupported`
- 索引：`files` 上建了 courseId、categoryId 以及复合 `courseId+categoryId`，按课程列文件不必全表扫。

**分类表的一处实现选择**：方案写的是「全局默认 5 类 + 每门课可自定义」。实现时改成
**每门课在创建时就复制出自己的一套 5 个分类行**（`courseId` 均指向该课程），
好处是改一门课的分类不会串到别的课，代价是课程多了以后分类行数按 5 倍增长——
几千行对 IndexedDB 而言可以忽略。

每套分类都带一个 `key`（`slides`/`homework`/`note`/`exam`/`other`）指向内置关键词表，自动推断靠它。
用户自建的分类 `key` 为 `null`，不参与自动推断——因为没有任何词表能猜出「考研专项」该匹配什么文件名。
默认兜底是 `key === 'other'` 的那一条。

---

## 四、V1 功能清单

1. **导入**：拖拽 / 选文件 / 选整个文件夹
2. **预览确认**：导入后先看自动分类结果，可改，确认才入库
3. **自动分类**：关键词规则 → 课程（课程名+别名匹配）+ 分类（课件/作业/笔记/真题/其他词表）
4. **待整理区**：认不出课程的文件进这里，批量手动分配
5. **课程结构**：学期 → 课程 → 5 默认分类，均可自定义
6. **版本识别**：识别「最新/终版/修订/v2/(2)」打标，同文件组内最新版高亮
7. **检索**：按课程 / 分类 / 文件名关键词 / 时间筛选
8. **本地文本提取**：导入后**后台异步**提取 docx / pptx / xlsx / pdf / txt 的正文并存库，不阻塞界面
9. **内容搜索**：按文件**正文关键词**搜索，与文件名搜索同一入口（搜到后高亮所在片段）
10. **导出**：多选课程 → 打包 zip（JSZip）
11. **备份/恢复**：全量 zip（文件 + metadata.json）导出与导入还原
12. **PWA**：离线可用 + 添加到主屏

---

## 五、技术实现要点

### 分类引擎（classifier.js）

- **课程匹配**：文件名里出现「课程名」或「任意别名」即命中；命中多门时取别名最长的（最具体）。
- **分类推断**：按关键词词表判断：
  - 课件：课件 / PPT / 讲义 / 第X章 / lecture / slide / 教学
  - 作业：作业 / homework / assignment / 报告 / due
  - 笔记：笔记 / note / 整理 / 总结 / 复习
  - 真题：真题 / 试卷 / 考试 / 期末 / 期中 / exam / quiz
  - 其他：都不命中时兜底
  - （词表实现时再细调，机制如上）

### 提取引擎（extractor.js）

**核心洞察：docx / pptx / xlsx 本质都是 ZIP 包里的 XML**，而 JSZip 已在技术栈内，所以这几种格式的提取不需要任何新依赖。

| 格式 | 做法 |
|---|---|
| txt / md | 直接读为文本 |
| docx | JSZip 解包 → 读 `word/document.xml` → 抽取文本节点 |
| pptx | JSZip 解包 → 按 `presentation.xml` + rels 定出**幻灯片顺序** → 逐页读 `ppt/slides/slideN.xml` 的 `<a:t>` 文本（顺序不能靠文件名排，否则页序会乱） |
| xlsx | JSZip 解包 → 读 `xl/sharedStrings.xml` + `sheet1.xml` |
| pdf | **pdf.js** 逐页 `getTextContent()`；pdf.js 约 1MB，**按需懒加载**，只有导入 PDF 时才拉 |
| 图片 / 扫描版 PDF | 提取不到文本，标 `extractStatus = empty`（**不做 OCR**） |

**实现约束：**

- 提取在**入库后**后台异步跑，可中断、可重试；导入界面不等待提取完成。
- 单个文件提取失败只标 `failed`，不影响其它文件、不影响文件本身可用。
- 公式、图表、排版在提取中会丢失——**这对搜索无影响**（要搜的关键词在正文文本里），本设计的用途也不是还原版式。
- **绝不把提取结果发到任何地方**，只存在本机 IndexedDB。

### 内容搜索

- 中文没有词边界，**子串匹配即可**，不需要分词或全文索引；几百到几千份文件直接线性扫描足够快。
- 搜索入口与文件名搜索统一：匹配到文件名的排在前面，匹配到正文的列在后面并显示命中片段。
- `extractStatus = empty` 的文件标注「无正文（图片/扫描件）」，避免用户误以为搜索漏了。

### 版本识别

1. 版本词表：最新、最终、终版、定稿、final、latest、修订、rev、v2/v3…、(2)(3) 等。
2. `baseName` = 文件名去掉版本词、去掉扩展名后的标准化名。
3. 同一 `courseId + categoryId` 下、baseName 相同的文件视为「同一文件的不同版本」。
4. 命中「最新/最终/终版/定稿」的标记 `latest`；否则版本号最大的标 `latest`；其余 `old`；不重复的 `none`。

### 备份恢复

- **导出**：zip 内结构为 `<学期>/<课程>/<分类>/<文件名>` + 根目录 `metadata.json`（含学期/课程/分类/别名/版本标记）。
- **恢复**：选 zip → JSZip 解包 → 读 metadata.json → 重建学期/课程/分类 → 文件按标记入库。
- 备份包**只含原始文件 + 元数据，不含提取出的正文**（否则备份体积翻倍）；恢复后重新提取一次即可。

---

## 六、预期目录结构

```
course-library/
├── index.html          入口页
├── manifest.json       PWA 清单
├── sw.js               Service Worker（离线缓存）
├── css/style.css       样式
├── js/
│   ├── app.js          主逻辑（视图/导入/预览/课程/检索/设置）
│   ├── db.js           IndexedDB 封装（七张表 + 游标扫描）
│   ├── classifier.js   分类引擎（课程/分类/版本）
│   ├── extractor.js    正文提取引擎（docx/pptx/xlsx/pdf/txt）
│   ├── search.js       检索（文件名 + 正文）
│   ├── seed.js         默认分类、常用课程别名表、分类关键词权重表
│   ├── exporter.js     导出/恢复（zip）
│   └── util.js         工具（文件名解析、格式化）
├── vendor/
│   ├── jszip.min.js            本地打包/解包库（导出 + OOXML 提取共用）
│   ├── pdf.min.mjs             pdf.js 主库（懒加载，仅提取 PDF 时使用）
│   ├── pdf.worker.min.mjs      pdf.js 的 worker
│   └── cmaps/                  中文 PDF 的 CID 字体映射表（169 份，约 1.5MB）
├── icons/              App 图标（由 scripts/make-icons.mjs 生成）
├── docs/screenshots/   README 用的截图
├── scripts/
│   ├── serve.js        本地静态服务器
│   ├── smoke.mjs       端到端冒烟测试（CDP 驱动无头 Chrome）
│   ├── test-classifier.mjs  分类引擎单元测试
│   ├── make-fixtures.mjs    生成冒烟测试用的假文件
│   └── make-icons.mjs       生成 PWA 图标
├── DESIGN.md           本文件
├── README.md           使用说明（含导入引导）
└── LICENSE             MIT
```

**关于 `vendor/cmaps/`**：中文 PDF 普遍用 CID 字体，pdf.js 没有这份映射表就抽不出文字，
而且是**静默失败**——用户只会看到「这份 PDF 搜不到东西」，完全无从排查。
1.5MB 对一个仓库不算什么，所以直接一并收进仓库，加载时按需取用（不进 Service Worker 预缓存）。

---

## 七、明确不做（Out of scope，v1）

- 自动扫描 QQ / 微信 / 钉钉本地缓存（浏览器读不到；微信文件还 `.dat` 加密）
- 板书照片 OCR / 图像增强（扫描版 PDF 同样不 OCR，只标「无正文」）
- 课表联动、作业 deadline 提醒
- 重复文件清理、旧版删除
- 云同步 / 多端自动同步 / 社区

### 已评估并明确排除：接入 AI 大模型生成复习笔记

**2026-09-16 讨论后决定不做。** 记录结论与理由，避免以后重复讨论：

- 它其实是三个独立功能：① 本地提取（低成本）② 接 API 重组为笔记 ③ 生成测验/闪卡。本次只保留 ①，②③ 均不做。
- **隐私冲突**：本产品的核心承诺是「无账号、无服务器、不上传任何文件」，而 AI 总结**必然要上传**，会动到产品地基。
- **公式是致命伤**：工程力学、高数这类课件的核心是公式，而纯文本提取会把公式变成乱码或丢失。做搜索无所谓（关键词在正文里），做「喂给模型总结」就会产出内容残缺、似是而非的笔记。要解决只能走「页面渲染成图片 → 多模态模型」，成本高好几倍。
- **产出物价值存疑**：AI 生成的笔记是模型的理解而非用户自己的，真实场景常是「生成一份漂亮笔记，然后再也没打开」。学习科学上「主动回忆（做题/自测）」效果远优于「重读笔记」。
- **若将来重启**：已验证**月之暗面 Kimi（api.moonshot.cn）支持浏览器 CORS 直连**，纯前端 PWA 可直连其 API，无需自建代理——这是当时最大的技术未知项。

---

## 八、里程碑（2026-09-17 起）

1. **骨架**：index.html + manifest + 目录结构 + 空状态 UI
2. **数据层**：IndexedDB 封装 + 学期/课程/分类 CRUD
3. **导入闭环**：导入 + 预览确认 + 分类引擎 + 待整理区
4. **版本 + 检索**：版本打标 + 按课程/分类/文件名/时间筛选
5. **导出/备份**：导出 zip + 备份恢复
6. **文本提取 + 内容搜索**：extractor.js（含 pdf.js 懒加载）+ 后台异步提取 + 正文搜索
7. **PWA + 上线**：sw.js + README + 冒烟测试 + 截图 + 部署 GitHub Pages

> 第 6 步排在核心里程碑之后：**它是可加可减的增量模块**，前面的核心闭环不依赖它。若时间紧，第 6 步整体后移也不影响前 5 步构成一个可用产品。

---

## 九、实现记录（2026-09-16）

七步全部完成。这部分记录**实现与原方案的偏差**，以及**只靠读代码发现不了、跑起来才暴露的 bug**。

### 与原方案的偏差

| 处 | 原方案 | 实现 | 原因 |
|---|---|---|---|
| 存储表 | 4 张，blob/正文内联在 files | 7 张，拆出 blobs / texts | `getAll` 会把 2GB 一次性读进内存（见第三节） |
| 分类 | 全局默认 + 课程可自定义 | 每门课各自持有一套分类行 | 避免改一门课的串到别的课 |
| 版本分组 | 方案未明确分组键 | `courseId::categoryId::baseKey` | 同一个 `复习笔记` 在「高数·笔记」和「大物·笔记」下是两份无关的文件 |
| 分类推断 | 「取首个命中的关键词」 | 加权累加取最高分 | 「期末复习笔记」同时命中「期末」（真题）和「复习/笔记」，首命中会判错；加权后笔记 8 分 > 真题 2 分 |
| 正文上限 | 未提 | 单文件 200 万字截断 | 防一份超大文件把内存和库撑爆 |
| pdf.js 配置 | 未提 | 带 `cMapUrl` + `cMapPacked` | 中文 PDF 不配这个会**静默**抽不出文字 |

### 冒烟测试抓出来的 bug

这些都不是语法错误，静态检查全绿，是 `scripts/smoke.mjs` 在真实浏览器里跑完整链路才暴露的：

1. **版本标记从来没生效过**（最严重）。`commitImport` 里先调 `refreshVersions` 再 `loadAll`，
   而 `refreshVersions` 是在 `state.files` 上分组的——那一刻新导入的文件还没进 `state.files`，
   等于在旧列表上算版本，永远算出空。修法是调换顺序。
2. **「目录名参与识别」是哑的**。`splitExt` 用 `lastIndexOf('.')` 切扩展名，
   而识别时传进去的可能是「文件名 + 目录名」（`笔记.doc 高等数学 作业`），
   最后一个点在 `.doc` 上，后面的目录名整段被当成扩展名丢掉。
   改成用正则只匹配**结尾处**的扩展名。
3. **拖动文件到别的课程后，原课程的版本标记不刷新**。收集受影响课程时读的是**已被改写**的
   `courseId`，原课程因此不在集合里；某个分组从 3 份减到 1 份时，会残留一个错误的「最新版」标签。
4. **预览里「未分类」看不出来**。分类一个词都没命中时 `categoryId` 是 `null`，
   而下拉框没有空选项，浏览器默认显示第一项，看着像已经选好了，实际存进去的还是 `null`。
5. **Service Worker 根本没注册上**。注册代码写在 `window.load` 的回调里，
   而 `boot()` 是异步的——等它 `await` 完 IndexedDB，`load` 事件早就过去了，
   监听器加在一个永远不会再触发的事件上。结果「离线可用」只是个写在 README 里的说法。
   修法是先判断 `document.readyState === 'complete'`，已经过了就直接注册。
6. 另有若干渲染与选择器问题（`renderCourse` 返回字符串而非赋值、`renderUnfiled` 按已废弃的
   分类模型过滤、`openCategoryManager` 里局部 `render` 遮蔽全局函数等），在自查阶段修掉。

### 验证情况

- `node scripts/test-classifier.mjs` —— 分类引擎单元测试，**26 通过 / 0 失败**
- `node scripts/smoke.mjs` —— 无头 Chrome 跑完整链路，**57 通过 / 0 失败**，全程零未捕获异常

冒烟测试覆盖：空状态引导 → 建学期 → 建课程（别名自动填充）→ 导入 8 个文件并核对每一条自动归类
→ 未识别文件拦截确认 → 落库渲染 → 三个同名笔记的版本分组 → 8 份文件全部提取成功
→ 五路正文搜索（txt / docx / pptx / xlsx / pdf）→ 导出备份 zip → **断网重载后数据仍在** → 异常检查。

最后一项值得单独说：它用 `Network.emulateNetworkConditions` 真的把网断掉再 `Page.reload`，
确认应用外壳从 Service Worker 缓存里起得来、课程和文件列表照常渲染。
上面第 5 条 bug 就是被这一项抓出来的——在那之前，所有测试全绿，而「离线可用」其实从未生效。

### 第二轮回查（2026-09-16 晚）

上线后把「用户会因此丢数据」的路径逐条核实了一遍，改动如下。

**先修的三个真问题：**

7. **`importBackup` 先清空再解包**。清空是不可逆的，而选错文件是最常见的失败：
   把「导出全部资料」的包当成备份、或者下载没下完的截断 zip，都会在清空之后才报错，
   用户一学期的资料就这么没了。现在在动手之前先拿 zip 的目录索引核对
   `metadata.json` 里登记的文件名——这一步不解压任何东西，几乎不花时间，
   但能在毁掉数据之前喊停（缺超过 20% 就拒绝）。测试里专门放了两个坏包验证它。
8. **正文提取队列会被一次失败彻底卡死**。内层 `catch` 里那句「把失败结果记下来」自己也会失败，
   而它在 `try` 之外——如果失败原因正是配额不足，这句会再抛一次，异常一路逃出
   `runExtractionQueue`（`boot()` 调用时没接），变成没人处理的 rejection；
   循环中断后 `extracting.running` 停在 `true`，**队列从此再也不跑**。
   现在失败记录单独兜一层，`running` 复位放到 `finally`，入口统一包一层 catch。
9. **`putTx` 的「要么全有要么全无」是纸面上的**。往事务里写的那一排 `store.put` 中间若有同步抛出
   （如 `DataCloneError`），异常会直接穿过调用者，而事务还活着、会在空闲时自动提交——
   前面几条写进去了、后面几条没有，调用方以为整批失败，库里其实改了一半。
   改成显式 `tx.abort()`（`js/db.js` 的 `runInTx`），删除路径同理：删一半比不删更糟。

**顺手补上的缺口：**

- **导入时配额爆掉，进度框永远挂在那儿**。模态进度框不走，用户只能刷新，还不知道进了几个。
  现在兜住异常，提示「只导入了 N/M 个，已导入的不会丢」，并把已写入的部分读回来重算版本
  （不读回来的话用户会以为一个没进，再导一次就重复了）。
- **`navigator.storage.persist()` 从没被调用过**。默认情况下浏览器把本站数据当缓存，
  磁盘紧张时可以直接清掉。现在库里已经有东西之后会申请一次持久化豁免
  （时机是刻意的：Firefox 为此弹权限框，第一次打开就弹会吓到人），设置页里也留了手动按钮。
- **设置页的「占用空间」是文件大小加起来估的**，永远小于浏览器实际扣掉的（元数据、索引、
  正文都占地方），报个小数字反而害人。改成问 `navigator.storage.estimate()`，
  接近 80% 时给出警告。
- **弹窗栈**。嵌套弹窗时按一次 Esc 会把整摞关掉（「编辑课程 → 删除确认」会连没保存的编辑一起丢），
  且 `close()` 不摘 keydown 监听，每开一个弹窗永久漏一个。现在只关最上面那层，并在 close 里摘监听。
- **学期删不掉**。打错字或重复建的学期会一直挂着。现在可以删，确认框写明会连带删掉几门课几个文件，
  并提示「只想挪课程的话改课程的所属学期即可」。
- **重复导入没有任何提示**。现在预览里对「同名同大小」的文件标「可能重复」，并给一个
  「排除 N 个重复项」的按钮。**默认行为不变，仍然照常导入**——同名同大小也可能是两份都该留的文件
  （同一份课件改个错字又存回来），替用户丢东西比不提醒更糟。

**测试补充**：备份 → 恢复的往返（含两个必须被拒收的坏包）、写满时的表现（用故障注入造
`QuotaExceededError`）、弹窗栈的 Esc、删除学期、重复提示。

> 关于「存储写满」这项测试：本来想用 CDP 的 `Storage.overrideQuotaForOrigin` 把配额压小，
> 试下来这个实验性命令对 IndexedDB 不生效——把配额压到 1KB 再重载，3KB 的文件照样写得进去，
> 它只影响 `estimate()` 报出来的数。真塞几 GB 不现实，所以改成拦下写 `blobs` 表的动作、
> 抛出 Chrome 配额爆掉时那个异常形状。它验的是「撞上配额之后应用怎么办」，而那正是要保证的事。
> 第 9 条 bug 就是被这个测试逼出来的。

同一套断言也能直接打线上站点（`BASE_URL=https://fcyzjbn.github.io/course-library/ node scripts/smoke.mjs`），
部署后实测 **30 通过 / 0 失败**。确认线上可用的方式是「跑一遍真实链路」，
而不是「curl 一下 index.html 返回 200」——后者只能说明文件传上去了。

测试用的 8 个假文件由 `scripts/make-fixtures.mjs` 现场生成，**是真实格式**：
docx/pptx/xlsx 是手写 ZIP 打包的真实 OOXML（不引第三方库，Node 自带能力足够），
PDF 也是按 xref 表算好偏移量拼出来的合法文件。
其中 pptx 故意让 `presentation.xml` 里声明的页序与文件名顺序**相反**，
用来验证提取引擎是真读了 `sldIdLst`，而不是偷懒按文件名排序。
